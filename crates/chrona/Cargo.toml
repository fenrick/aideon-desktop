[package]
name = "aideon_chrona"
version = "0.0.0"
edition = "2024"
authors = ["Aideon Chrona Contributors"]
license = "MIT"

[dependencies]
aideon_praxis = { path = "../praxis" }
log = "0.4"

[dev-dependencies]
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt"] }
